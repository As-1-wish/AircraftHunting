const showPanel_height = 650;   // 屏幕尺寸
const showPanel_width = 1050;
const train_height = 700;   // 训练尺寸
const train_width = 700;
const init_speed = 20;
const rank_height = (showPanel_height/train_height);   // 转换比例
const rank_width = (showPanel_width/train_width);

const friendly_plane_prefix = "friendly-plane-";
const enemy_plane_prefix = "enemy-plane-";
const friendly_keyframes_prefix = "friendly-fly-";
const enemy_keyframes_prefix = "enemy-fly-";
const friendly_container_prefix = "friendly-container-";
const enemy_container_prefix = "enemy-container-";